---
title: KCSE Capture
description: Fetch KCSE results for individual candidates or batch uploads.
template: splash
---

import KcseResultsApp from '../../components/KcseResultsApp.astro';

<div class="kcse-full-bleed">
  <KcseResultsApp />
</div>

<style is:global>{`
  .kcse-full-bleed {
    width: 100vw;
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
  }

  .kcse-full-bleed > * {
    max-width: 100%;
  }
`}</style>

## Notes

- Ensure the FastAPI backend is running and reachable at the API base URL.
- For local SSL bypass, start the backend with `KNEC_VERIFY_SSL=false`.
- Batch uploads require `python-multipart` and `pdftotext` on the backend host.
